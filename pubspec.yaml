name: cleanjet
description: A CLI tool for initializing and managing Flutter projects with a clean structure.
version: 2.0.0
repository: https://github.com/Mahamudul-Dev/cleanjet

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  args: ^2.6.0
  io: ^1.0.4
  recase: ^4.1.0
  yaml: ^3.1.2
  yaml_edit: ^2.2.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
